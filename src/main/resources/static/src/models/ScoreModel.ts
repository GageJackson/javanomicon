class ScoreModel {
    quantity: number;
    quality: number;

    constructor(
            quantity: number,
            quality: number
    ) {
        this.quantity = quantity;
        this.quality = quality;
    }
}

export default ScoreModel;